services:
  khmer-calendar-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: khmer-calendar-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Phnom_Penh
      - NODE_ENV=production
